{{ form_start(postForm) }}
  {{ form_row(postForm.categories) }}
  {{ form_row(postForm.title) }}
  {{ form_row(postForm.body) }}
  {{ form_row(postForm.img) }}
  {{ form_row(postForm.send) }}
  {{ form_row(postForm._token) }}
{{ form_end(postForm, {render_rest: false}) }}